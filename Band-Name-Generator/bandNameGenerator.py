# Basic print / showing output using print
print("Welcome to the Band Name Generator.")

# User input
# It also can be cityName = input() / petName = input()
cityName = input("What's name of the city you grew up in? ")
petName = input("What's your pet's name? ")

# Printing the output using the formatting function
print("Your band name could be {} {}".format(cityName, petName))
